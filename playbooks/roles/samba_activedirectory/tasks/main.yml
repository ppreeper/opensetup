---
- name: smbd
  ansible.builtin.systemd:
    name: smbd
    state: stopped
    enabled: no
    masked: yes
- name: nmbd
  ansible.builtin.systemd:
    name: nmbd
    state: stopped
    enabled: no
    masked: yes
- name: winbind
  ansible.builtin.systemd:
    name: winbind
    state: stopped
    enabled: no
    masked: yes
- name: samba-ad-dc
  ansible.builtin.systemd:
    name: samba-ad-dc
    enabled: yes
    masked: no
