---
# requirement
- name: Git
  ansible.builtin.apt:
    name:
      - git
      - jq
      - wget
    state: present
  when: ansible_os_family == 'Debian'

- name: Install gup
  ansible.builtin.template:
    src: "gup"
    dest: "/usr/local/bin/gup"
    mode: '0775'

