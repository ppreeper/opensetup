---
- name: CLI remote file systems
  ansible.builtin.apt:
    name:
      - rsync
      - sshfs
      - nfs-common
      - samba
      - cifs-utils
      - krb5-user
    state: present
  when: ansible_os_family == 'Debian'

- name: CLI helpers
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
    owner: "{{ USER }}"
    group: "{{ USER }}"
  with_items:
    - { src: "smnt", dest: "{{ USERBIN }}", mode: "0755" }
    - { src: "sumnt", dest: "{{ USERBIN }}", mode: "0755" }
    - { src: "nmnt", dest: "{{ USERBIN }}", mode: "0755" }
    - { src: "numnt", dest: "{{ USERBIN }}", mode: "0755" }
    - { src: "smbmnt", dest: "{{ USERBIN }}", mode: "0755" }
    - { src: "smbumnt", dest: "{{ USERBIN }}", mode: "0755" }
